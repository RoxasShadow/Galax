<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="net.giovannicapuano.galax"
  android:versionCode="1"
  android:versionName="1.0" >

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

  <uses-sdk
    android:minSdkVersion="9"
    android:targetSdkVersion="19" />

  <application
    android:allowBackup="true"
    android:icon="@drawable/ic_launcher"
    android:label="@string/app_name"
    android:theme="@style/AppTheme" >
    <activity
      android:name="net.giovannicapuano.galax.LoginActivity"
      android:label="@string/app_name"
      android:noHistory="true" >
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity
      android:name="net.giovannicapuano.galax.MainActivity"
      android:label="@string/app_name"
      android:noHistory="true" >
    </activity>
    <activity
      android:name="net.giovannicapuano.galax.SignupActivity"
      android:label="@string/title_activity_registration"
      android:noHistory="true" >
    </activity>
    <activity
      android:name="net.giovannicapuano.galax.ChatActivity"
      android:label="@string/title_activity_chat"
      android:noHistory="true" >
    </activity>
  </application>

</manifest>